{
  "name": "macpro-wms-connector",
  "version": "0.1.0",
  "description": "WMS Oracle CDC to BigMAC Kafka via Debezium and Kafka Connect",
  "main": "lib/index.js",
  "scripts": {
    "preinstall": "node -e \"if (process.env.npm_config_user_agent && process.env.npm_config_user_agent.startsWith('npm/')) { console.error('This project uses Yarn. Use: yarn install'); process.exit(1); }\"",
    "build": "tsc",
    "synth": "yarn build && yarn exec cdk synth",
    "cdk": "yarn build && yarn exec cdk"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.170.0",
    "aws-sdk": "^2.1691.0",
    "constructs": "^10.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "aws-cdk": "^2.170.0"
  },
  "engines": {
    "node": ">=18"
  }
}
